type Query {
  info: String!
  me: Player!
  games: [Game!]!
  players: [Player!]!
}

type Mutation {
  playing(_id: String!, playingToday: Boolean!): Player!
  reset: Boolean!

  createPlayer(email: String!): Player!
  deletePlayer(_id: String!): Boolean
}

type Subscription {
  statusChange: [Player]!
}

type Game {
  _id: String!
  name: String!
  notes: String
  players: [Recommendation!]
}

type Recommendation {
  num: Int!
  best: Float!
  not: Float!
  recommended: Float!
}

input RecommendationInput {
  num: Int!
  best: Float!
  not: Float!
  recommended: Float!
}

type Player {
  _id: String!
  email: String!
  playingToday: Boolean!
}
