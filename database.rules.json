{
  "rules": {
    "games": {
      // /games is world readable
      ".read": "true",
      ".write": "auth != null"
    },
    "players": {
      // /players is world readable
      ".read": "true",

      "$uid": {
        ".write": "auth.uid === $uid"
      }
    }
  }
}
